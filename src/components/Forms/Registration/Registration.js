import React from 'react';

const Registration = () => {
    return (
        <div>
            <span>Вiтаем !</span>

            <input type="login"  name="password" placeholder="Увядзiце почту" />
            <input type="name"  name="password" placeholder="Увядзiце iмя" />
            <input type="password"  name="password" placeholder="Прыдумайце пароль" />

            <button type="submit">Стварыць</button>

        </div>
    );
};

export default Registration;